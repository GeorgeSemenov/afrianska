mixin page-title__header(modifier)
  if modifier.header == undefined
      - modifier.header = 'someValue';
  h3.page-title__header(class="main-content-frames")&attributes(attributes)!=modifier.header
